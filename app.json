{
	"name": "Bug test",
	"stack": "heroku-16",
	"addons": [
		"heroku-postgresql:hobby-dev"
	],
	"buildpacks": [
		{
			"url": "https://github.com/vapor-community/heroku-buildpack"
		}
	],
	"env": {
		"ENV_TYPE": {
			"description": "Environment type. Valid values are: `production`, `development`",
			"required": false,
			"value": "production"
		}
	}
}
